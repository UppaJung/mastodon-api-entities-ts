# Mastodon API TypeScript Interface Generator

Generates TypeScript interfaces for the entities returned by the Mastodon API by
scraping and parsing the web pages describing those entities.

```
deno task build
```

